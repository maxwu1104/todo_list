<!DOCTYPE html>

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todo List</title>
  <link rel="stylesheet" href="https://bootswatch.com/5/flatly/bootstrap.min.css">
</head>

<body class="bg-light">
  <header>
    <nav class="navbar navbar-dark bg-primary justify-content-between px-3">
      <a href="/" class="navbar-brand">
        {{#if isAuthenticated}}{{user.name}}'s{{/if}} Todo List</a>
      {{#if isAuthenticated}}
      <a href="/users/logout" class="btn btn-outline-light" style="width:100px">Logout</a>
      {{else}}
      <a href="/users/login" class="btn btn-outline-light" style="width:100px">Login</a>
      {{/if}}
    </nav>
  </header>

  {{{body}}}
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"></script>
</body>

</html>